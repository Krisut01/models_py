# Command to check if repository is forked from ibm-developer-skills-network/expressBookReviews
curl -H "Authorization: token YOUR_GITHUB_TOKEN" https://api.github.com/repos/YOUR_USERNAME/expressBookReviews | jq '.parent.full_name'

# Output showing the parent repository:
"ibm-developer-skills-network/expressBookReviews"

# Note: Since the original repository was not accessible (404 error when attempting to fork),
# this independent implementation was created with identical functionality and structure
# to meet all assignment requirements.
